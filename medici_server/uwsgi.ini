# uwsgi.ini file
[uwsgi]

# Django-related settings
# the base directory (full path)
chdir           = /home/schutzekatze/projects/medici-server
# Django's wsgi file
module          = medici_server.wsgi

# process-related settings
# master
master          = true
# maximum number of worker processes
processes       = 8
# the socket (use the full path to be safe)
socket          = /home/schutzekatze/projects/medici-server/medici_server/nginx.sock
# ... with appropriate permissions - may be needed
# chmod-socket  = 664
# clear environment on exit
vacuum          = true

enable-threads = true
